using UnityEngine;

public static class UnitCreator
{
    //public static AutoBattlerUnit CreateUnit(UnitSaveData data, Transform parent)
    //{
    //    Debug.Log("Trying to load prefab: Units/" + data.prefabName);

    //    GameObject prefab = Resources.Load<GameObject>("Units/" + data.prefabName);
    //    GameObject obj = GameObject.Instantiate(prefab, parent);

    //    UnityEngine.Object.DontDestroyOnLoad(obj);

    //    AutoBattlerUnit unit = obj.GetComponent<AutoBattlerUnit>();

    //    unit.level = data.level;
    //    unit.skillPoints = data.skillPoints;
    //    unit.maxHealth = data.maxHealth;
    //    unit.damage = data.damage;
    //    unit.range = data.range;
    //    unit.thinkInterval = data.thinkInterval;

    //    return unit;
    //}
}
